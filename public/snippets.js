let actions = {
	"webpages": [
		{"title": "YouTube", "url": "youtube.com"}, 
		{"title": "Google",  "url": "google.com"}, 
		{"title": "GitHub",  "url": "github.com"}
	],

	"apps": [
		{"title": "Chromium", "cmd": "chromium"},
		{"title": "Visual Studio Code", "cmd": "code"}
	],

	"snips": [
		"t = int(input())\n",
		"for _ in range(t):\n",
		"for _ in range(1, t+1):\n",
		"for t in range(T):\n",
		"for t in range(1, T+1):\n",
		"n = int(input())\n",
		"[int(s) for s in input().split()]",
		"for i in range(n):\n",
		"for i in range(n):\nprint(n)\n",
		"for j in range(m):\n"
	]
};
